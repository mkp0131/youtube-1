extends base
block content
  form(method="POST")
    input(type="text", name="title" placeholder="제목을 입력해주세요.")
    input(type="text", name="description" placeholder="설명을 입력해주세요.")
    input(type="text" name="hashtags" placeholder="해쉬태그를 입력해주세요.")
    button(type="submit") Upload